import { Item } from './types';
declare abstract class Sorter {
    direction: SortDirection;
    constructor(direction: SortDirection);
    protected abstract comparer(a: Item, b: Item): number;
    sort(items: Item[]): Item[];
}
export declare enum SortStrategy {
    Area = 0,
    ShortSide = 1,
    LongSide = 2,
    Perimeter = 3,
    Differences = 4,
    Ratio = 5
}
export declare enum SortDirection {
    ASC = 0,
    DESC = 1
}
export declare function GetSortImplementation(strategy: SortStrategy, direction: SortDirection): Sorter;
export {};
