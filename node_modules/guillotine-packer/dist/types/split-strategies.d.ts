import { Rectangle, Item } from './types';
declare abstract class Splitter {
    kerfSize: number;
    constructor(kerfSize: number);
    abstract split(rectangle: Rectangle, item: Item): Rectangle[];
    protected splitHorizontally(rectangle: Rectangle, item: Item): Rectangle[];
    protected splitVertically(rectangle: Rectangle, item: Item): Rectangle[];
}
export declare enum SplitStrategy {
    LongLeftoverAxisSplit = 0,
    ShortLeftoverAxisSplit = 1,
    LongAxisSplit = 2,
    ShortAxisSplit = 3
}
export declare function GetSplitImplementation(strategy: SplitStrategy, kerfSize: number): Splitter;
export {};
