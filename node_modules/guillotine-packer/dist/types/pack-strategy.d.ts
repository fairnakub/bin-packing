import { Item } from './types';
import { SelectionStrategy } from './selection-strategies';
import { SortDirection, SortStrategy } from './sort-strategies';
import { SplitStrategy } from './split-strategies';
export declare type PackStrategyOptions = {
    binHeight: number;
    binWidth: number;
    items: Item[];
    selectionStrategy: SelectionStrategy;
    splitStrategy: SplitStrategy;
    sortStrategy: SortStrategy;
    sortOrder: SortDirection;
    kerfSize: number;
    allowRotation: boolean;
};
export declare type PackedItem = {
    item: any;
    width: number;
    height: number;
    x: number;
    y: number;
    bin: number;
};
export declare function PackStrategy({ binHeight, binWidth, items, selectionStrategy, splitStrategy, sortStrategy, sortOrder, kerfSize, allowRotation }: PackStrategyOptions): {
    sortStrategy: SortStrategy;
    sortOrder: SortDirection;
    packedItems: PackedItem[][];
    splitStrategy: SplitStrategy;
    selectionStrategy: SelectionStrategy;
};
