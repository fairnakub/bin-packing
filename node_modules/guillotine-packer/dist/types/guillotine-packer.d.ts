import { SplitStrategy } from './split-strategies';
import { SelectionStrategy } from './selection-strategies';
import { SortStrategy } from './sort-strategies';
import { PackedItem } from './pack-strategy';
import { Item } from './types';
declare type PackerInputs = {
    binHeight: number;
    binWidth: number;
    items: Item[];
};
declare type PackerConfig = {
    selectionStrategy?: SelectionStrategy;
    splitStrategy?: SplitStrategy;
    sortStrategy?: SortStrategy;
    kerfSize?: number;
    allowRotation?: boolean;
};
declare type PackerResult = PackedItem[][] | null;
declare function Packer({ binHeight, binWidth, items }: PackerInputs, { selectionStrategy, splitStrategy, sortStrategy, kerfSize, allowRotation }?: PackerConfig): PackerResult;
export { SplitStrategy, SelectionStrategy, SortStrategy, Packer as packer };
